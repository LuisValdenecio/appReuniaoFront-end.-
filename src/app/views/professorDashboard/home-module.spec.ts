import { HomeModuleProfDash } from '../professorDashboard/home-module';

describe('HomeModule', () => {
  it('should create an instance', () => {
    expect(new HomeModuleProfDash()).toBeTruthy();
  });
});
